{
  "name": "vridhajan-sahyog",
  "version": "1.0.0",
  "main": "index.js",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently \"npm start --prefix backend\" \"npm start --prefix event-frontend\"",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm start --prefix event-frontend\"",
    "backend": "npm start --prefix backend",
    "frontend": "npm start --prefix event-frontend",
    "build": "npm run build --prefix event-frontend",
    "install-all": "npm install && npm install --prefix backend && npm install --prefix event-frontend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "bcryptjs": "^3.0.2",
    "build": "^0.1.4",
    "cors": "^2.8.5",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.16.3",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.37.7"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
